# module-ballerinax-cdc.schema.azure.blob.driver
This library provides the drivers needed to use Azure Blob storage as an internal schema history storage mechanism for the Ballerina CDC (Change Data Capture) connector.
